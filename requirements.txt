# Requirements for metagenomics-fm
# Python 3.12
#
# Two separate environments are needed due to a torch version conflict
# between TabICL/TabDPT (cu129) and ContextTab (cu128).

# ENVIRONMENT 1 — main (RF, XGBoost, TabPFN, TabICL, TabDPT)

# PyTorch (CUDA 12.9):
# pip install torch==2.10.0+cu129 torchvision==0.25.0+cu129 --index-url https://download.pytorch.org/whl/cu129

# TabICL — pinned commit used in experiments:
# pip install -e git+https://github.com/soda-inria/tabicl@d5d752c48e63fadeb635d8cdc03c91866051ee38#egg=tabicl

# TabDPT — try PyPI first, fallback to GitHub:
# pip install tabdpt==1.1.12
# pip install git+https://github.com/layer6ai-labs/TabDPT.git@v1.1.12

tabpfn==8.0.3
tabpfn-common-utils==0.2.21
scikit-learn==1.8.0
xgboost==3.2.0
lightgbm==4.6.0
MLstatkit
numpy>=2.4.0
pandas==3.0.1
pyarrow==23.0.1
fastparquet>=2024.0.0
scipy==1.17.1
matplotlib==3.10.8
seaborn==0.13.2
tqdm==4.67.3
joblib==1.5.3
shap==0.51.0

# ENVIRONMENT 2 — contextab (ContextTab only)

# PyTorch (CUDA 12.8):
# pip install torch==2.11.0+cu128 torcheval==0.0.7 --index-url https://download.pytorch.org/whl/cu128

# ContextTab — pinned commit used in experiments:
# pip install git+https://github.com/SAP-samples/sap-rpt-1-oss@4310453f3bbb9c9c403b029e966565e7fe652d97
